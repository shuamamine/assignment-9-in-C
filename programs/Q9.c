#include <stdio.h>
int main()
{
int n,i,j=0,k=0;
printf("enter limit of array");
scanf("%d", &n);
int a[n],b[n],c[n];
printf("enter values in array");
for(int i = 0; i < n; i++)
{
  scanf("%d", a[i]);
}
for(int i = 0; i < n; i++)
{
